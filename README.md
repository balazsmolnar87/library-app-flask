# library-app-flask
This is an app for a library to manage books

### Run in debug mode
`flask run --debug`

### Run Docker container 
`docker run -p 5000:5000 utamaro/library-app-flask`

### For Terraform
`cd terraform`

`terraform init`

`terraform plan`

If you are happy with the actions

`terraform apply`
